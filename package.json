{
  "name": "vivid",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vue-tsc -b && vite build && vite preview",
    "deploy": "vue-tsc -b && vite build && touch dist/.nojekyll && npx gh-pages -d dist --dotfiles"
  },
  "dependencies": {
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "@vitejs/plugin-vue": "^6.0.0",
    "@vue/tsconfig": "^0.7.0",
    "gh-pages": "^6.3.0",
    "sass": "^1.90.0",
    "tailwindcss": "^4.1.11",
    "typescript": "~5.8.3",
    "vite": "7.1.5",
    "vue-router": "^4.5.1",
    "vue-tsc": "^2.2.12"
  }
}
